# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: The Worker's Winter Olympiad 1931
email: k20111151@kcl.ac.uk
description: This project provides a digital archive for the 1931 Worker's Winter Olympiad that took place in Mürzzuschlag, Austria.
# >-this means to ignore newlines until "baseurl:" 
baseurl: "" # the subpath of your site, e.g. /blog
url: http://workerswinterolympiad1931.netlify.app 
# the base hostname & protocol for your site, e.g. http://example.com
twitter_username: ElisabethKoeni3
github_username:  Koenel

# Build settings
theme: minima
plugins:
  - jekyll-feed
  # I hope this will include my pages and make the more visible to finally use the css
include: [_pages]
collections:
  organiser:
    output: true
  participant:
    output: true
  spectator:
    output: true
# The next 2 steps are taken from https://www.taniarascia.com/make-a-static-website-with-jekyll/
# add Sass as css folder -  individual components will be compiled into css -- not sure if I will need this
sass:
  sass-dir: _sass


# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
